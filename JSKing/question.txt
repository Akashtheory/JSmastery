📋 Detailed List of 20 If–Else Questions
1. Voting Eligibility
Check if a person’s age is 18 or above → print “Eligible to vote” else “Not eligible”.

2. Metro Student Discount
Check if isStudent is true → print “Student discount applied” else “Regular fare”.

3. Temperature Comfort
If temperature > 30 → print “Hot” else “Cool”.

4. Exam Pass/Fail
If marks ≥ 35 → print “Pass” else “Fail”.

5. Internet Pack Validity
If daysLeft > 0 → print “Pack active” else “Recharge needed”.

6. Phone Battery Warning
If battery ≤ 20 → print “Low battery” else “Battery OK”.

7. Traffic Fine (Speed Limit)
If speed > limit → print “Over-speeding fine” else “Safe driving”.

8. Train Reservation Concession
If age ≥ 60 → print “Senior citizen concession” else “Regular fare”.

9. OTP Validation
If userOTP === actualOTP → print “Login success” else “Invalid OTP”.

10. Bus Ticket Fare
If age < 12 → print “Child fare” else “Adult fare”.

11. Festival Offer
If purchase amount ≥ 1000 → print “Coupon applied” else “Add more for coupon”.

12. Cricket Score Chase
If teamScore ≥ target → print “Chase successful” else “Need more runs”.

13. ATM Withdrawal
If balance ≥ withdraw → print “Cash dispensed” else “Insufficient funds”.

14. Cinema Age Restriction
If age ≥ 18 → print “Allowed (A-rated)” else “Not allowed”.

15. Rain Plan
If isRaining === true → print “Carry umbrella” else “Wear sunglasses”.

16. Mobile Data Usage
If usedGB ≥ capGB → print “Data cap reached” else “High speed active”.

17. Salary Tax Slab
If salary > 500000 → print “Higher tax slab” else “Basic tax slab”.

18. App Update Needed
If currentVersion < latestVersion → print “Update available” else “Up to date”.

19. Shop Open Hours
If hour between openHour and closeHour → print “Shop open” else “Shop closed”.

20. Discount Stackable
If hasCoupon === true and amount ≥ 1000 → print “Coupon applied” else “Conditions not met”.


................................................


📋 Full Conditional Questions List (16 total)
🟢 Else If Ladder (4 Questions)
Student Grade System Hint: Use else if ladder with ranges (≥85, ≥75, ≥60).

Temperature Band Hint: Compare ranges step by step (≥40, ≥30, ≥20).

Exam Result Hint: Ladder with thresholds (≥75, ≥60, ≥33).

Salary Tax Slab Hint: Use else if with salary ranges (>10L, >5L, else basic).

🔵 Nested If Else (4 Questions – Easy)
Login System Hint: Outer if = login check, inner if = role check.

Cricket Match Result Hint: Outer if = win/loss, inner if = quality of win.

ATM Withdrawal Hint: Outer if = PIN, inner if = balance.

School Admission Hint: Outer if = age, inner if = documents.

🟣 Switch Case (4 Questions)
Weekday Name Hint: Switch on day number (1–7).

Payment Method Hint: Switch on string value (“UPI”, “CARD”, “COD”).

Traffic Signal Hint: Switch on signal color (“RED”, “YELLOW”, “GREEN”).

Movie Ticket Type Hint: Switch on ticket string (“REGULAR”, “VIP”, “BALCONY”).

🔴 Advanced Nested If Else (4 Questions – Complex Logic)
Hospital Triage Hint: Outer if = critical check, inner if = moderate + age, else mild.

University Admission Hint: Outer if = marks ≥90, else if = 75–89 with category, else sports quota.

Online Order Delivery Hint: Outer if = address verified, inner if = payment success, inner‑inner if = stock.

Apartment Entry Hint: Outer if = resident vs guest, inner if = ID/pass, inner‑inner if = host confirmation.